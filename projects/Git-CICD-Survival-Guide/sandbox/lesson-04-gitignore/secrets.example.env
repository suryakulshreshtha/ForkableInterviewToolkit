# TEMPLATE ONLY -- committed on purpose. Contains no real values.
#
# Copy to secrets.env (which IS gitignored) and fill in locally:
#     cp secrets.example.env secrets.env
#
# Chapter 3 exercise: create secrets.env, put anything in it, then run
# `git status`. It should NOT appear. That is .gitignore working.

API_TOKEN=replace-me
DB_PASSWORD=replace-me
